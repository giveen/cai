---
applyTo: '**'
---

<todos title="Guard debug prints in model adapter" agentRequirement="Review steps frequently throughout the conversation and DO NOT stop between steps unless they explicitly require it. Keep the todos updated using the todo_write tool (do not edit this file).">
- [x] fix-challenge-key: Fix undefined `challenge_key` reference in `setup_ctf()` in util.py 🔴
  _Replaced reference with `raw_challenge_spec`-aware check and preserved fallback behavior._
- [x] add-missing-os-import: Add missing `import os` to `src/cai/sdk/agents/_run_impl.py` to avoid NameError 🔴
  _Added standard library import near other stdlib imports._
- [x] remove-duplicate-php-webshell: Consolidate duplicate `generate_php_webshell()` definitions in `src/cai/tools/web/webshell_suit.py` 🟡
  _Removed duplicated block, kept consolidated implementation, and fixed f-string issue._
- [x] tests-and-verify: Run pyflakes and the affected tests, and mark fixes verified 🟡
  _Ran static checks and `pytest tests/commands/test_command_parallel.py` (26 passed)._
- [x] fix-bare-fstring: Remove pointless f-string prefixes without placeholders in `src/cai/tools/web/webshell_suit.py` 🟢
  _Consolidated webshell module removed the stray f-string._
- [x] dedupe-memory-analysis-imports: Remove duplicate import blocks in `src/cai/agents/memory_analysis_agent.py` 🟡
  _Kept a single, clear import block to avoid redefinition warnings._
- [x] fix-repl-init-import-order: Make `src/cai/repl/__init__.py` robust to test import ordering by lazy-loading common submodules 🔴
  _Added module-level __getattr__ to import submodules like `commands` on attribute access, preventing collection-time ImportError._
- [x] tests-and-verify-4-6: Run pyflakes and collect-only pytest to validate fixes to import duplication and package init 🟡
  _Ran `python -m pyflakes src/cai/` and `pytest --collect-only` to ensure no collection errors._
- [-] guard-debug-print: Replace unconditional debug `print()` with `logger.debug()` in `src/cai/sdk/agents/models/openai_chatcompletions.py` 🔴
  _Use the module `logger` to avoid unguarded console output; preserve the diagnostic string._
</todos>

<!-- Auto-generated todo section -->
<!-- Add your custom Copilot instructions below -->
