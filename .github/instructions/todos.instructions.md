---
applyTo: '**'
---

<todos title="Diagnose litellm/openai streaming error" agentRequirement="Review steps frequently throughout the conversation and DO NOT stop between steps unless they explicitly require it. Keep the todos updated using the todo_write tool (do not edit this file).">
- [x] inspect-environment: Gather installed versions of `openai`, `litellm`, and check for `mcp` package in active venvs. 🔴
- [x] analyze-logs-and-code: Inspect logs and `src/cai/sdk/agents/models/openai_chatcompletions.py` to see how `tool_choice` is passed to litellm. 🟡
- [x] repair-mcp-or-disable: Either install `mcp` package in the runtime venv or configure/patch to avoid litellm importing experimental MCP modules. 🔴
- [ ] ensure-openai-compatibility: Pin or align `openai` and `litellm` versions in the runtime venv (e.g., `openai==1.75.0` & `litellm<1.83`) and reinstall if needed. 🔴
- [ ] test-streaming: Run a streaming request or relevant tests to reproduce the error and confirm the fix. 🟡
- [ ] commit-push: Commit any dependency or code changes and push to branch `tui`. 🟢
</todos>

<!-- Auto-generated todo section -->
<!-- Add your custom Copilot instructions below -->
