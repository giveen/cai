---
applyTo: '**'
---

<todos title="Propagate LOCAL_* envs to OPENAI_* for local proxy support" agentRequirement="Review steps frequently throughout the conversation and DO NOT stop between steps unless they explicitly require it. Keep the todos updated using the todo_write tool (do not edit this file).">
- [x] propagate-local-to-openai-env: Add startup logic to copy `LOCAL_API_BASE` → `OPENAI_API_BASE` and `LOCAL_API_KEY` → `OPENAI_API_KEY` when OPENAI_* envs are not set 🔴
  _Apply env propagation early in CLI entrypoint to ensure the OpenAI client is configured for the LiteLLM proxy._
- [ ] load-dotenv-if-present: Load `.env` at startup if python-dotenv is available so local env file values are applied 🟡
  _Optional: use python-dotenv to support .env files during local development._
- [x] remove-store-or-document-proxy: Decide whether CAI should stop sending `store` by default or document configuring the LiteLLM proxy DB/disable spend-logs 🟡
  _This determines whether we change request payloads or require proxy configuration._
- [ ] smoke-test-local-proxy: Run a smoke test: start CAI with propagated envs and verify requests hit proxy and that `No connected db.` no longer appears 🟡
  _Manual verification step after code change and/or proxy config change._
</todos>

<!-- Auto-generated todo section -->
<!-- Add your custom Copilot instructions below -->
