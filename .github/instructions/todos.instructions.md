---
applyTo: '**'
---

<todos title="Refactor: UI & Metrics Cleanup" agentRequirement="Review steps frequently throughout the conversation and DO NOT stop between steps unless they explicitly require it. Keep the todos updated using the todo_write tool (do not edit this file).">
- [x] refactor-metrics-finalize-session: Add `finalize_session(session_logger, start_time, idle_time)` to `cai/repl/ui/metrics.py` and move session finalization logic there. 🔴
  _Move format_time, total duration calc, and GLOBAL_USAGE_TRACKER.end_session into metrics.finalize_session; ensure console printing and keyboard-interrupt cleanup are handled there._
- [x] update-cli-keyboardinterrupt: Replace KeyboardInterrupt block in `cai/cli.py` with a single call to `finalize_session`. 🔴
  _Ensure `run_cai_cli` or main loop calls `finalize_session` on interrupt and on normal exit._
- [-] run-lint-and-tests: Run linter (`ruff --fix`) and `pytest -q` to validate changes and fix issues if needed. 🟡
  _Run tests to ensure behavior preserved after moving finalization code._
</todos>

<!-- Auto-generated todo section -->
<!-- Add your custom Copilot instructions below -->
